---
import { site } from '../lib/site';

type Props = {
  active?: 'visuals' | 'history' | 'contact';
};

const { active } = Astro.props;

const baseClass =
  'inline-flex items-center justify-center px-5 py-2 rounded-full text-sm text-black/65 no-underline min-w-[90px] transition-all duration-300 hover:text-black/85 hover:bg-white/20';
const activeClass = 'bg-white/95 text-black/85 hover:bg-white/95';
---

<nav
  class="fixed left-1/2 bottom-10 -translate-x-1/2 z-50 inline-flex gap-1 p-1 rounded-full bg-gray-300/80 backdrop-blur-xl"
  aria-label="Primary"
>
  <a
    href={site.routes.visuals}
    class:list={[baseClass, active === 'visuals' && activeClass]}
    aria-current={active === 'visuals' ? 'page' : undefined}
  >
    Visuals
  </a>
  <a
    href={site.routes.history}
    class:list={[baseClass, active === 'history' && activeClass]}
    aria-current={active === 'history' ? 'page' : undefined}
  >
    My History
  </a>
  <a
    href={site.routes.contact}
    class:list={[baseClass, active === 'contact' && activeClass]}
    aria-current={active === 'contact' ? 'page' : undefined}
  >
    Contact
  </a>
</nav>
